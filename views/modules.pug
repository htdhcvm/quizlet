include mixin/head
include mixin/footer


<!DOCTYPE html>
html(lang="en")
    head
        +head(dataHead)        
    body    
        div(class="wrapper-main")
            
            nav(class="haed")
                include includes/header.pug
            if dataHead.data
                div( class="menu")
                    ul
                        li 
                            a(href="/") Главная
                        li 
                            a(href="/modules") Модули
                div(class="content")
                    a(href="/modules/create") Создать
                    div(class="list-wrapper")
                        h2 Список модулей 

            div(id="wrapper-modal")

        +footer(dataHead.js)